"use client";
import {FormEvent,useState} from "react";
const roles=["담임목사","위임목사","대표목사","수석부목사","부목사","행정목사","목양목사","교육목사","강도사","전임전도사","교육전도사","전도사","협동목사","원로목사","은퇴목사"];
export default function MinistrySuggestionForm({churchId,churchName}:{churchId:number;churchName:string}){const [busy,setBusy]=useState(false),[message,setMessage]=useState("");async function submit(event:FormEvent<HTMLFormElement>){event.preventDefault();setBusy(true);setMessage("");const form=event.currentTarget,data=new FormData(form);try{const response=await fetch("/api/ministry-suggestions",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({churchId,name:data.get("name"),roleTitle:data.get("roleTitle"),sourceUrl:data.get("sourceUrl"),note:data.get("note"),company:data.get("company")})}),result=await response.json() as {error?:string};if(!response.ok)throw new Error(result.error||"제보하지 못했습니다.");form.reset();setMessage("검토 대기 목록에 안전하게 접수했습니다.");}catch(reason){setMessage((reason as Error).message)}finally{setBusy(false)}}return <details className="ministry-suggestion"><summary>빠진 교역자 알려주기</summary><form onSubmit={submit}><p>{churchName}의 공식 공개 정보만 알려 주세요. 확인 전에는 페이지에 표시하지 않습니다.</p><div><label><span>이름</span><input name="name" minLength={2} maxLength={60} required/></label><label><span>직책</span><select name="roleTitle" required><option value="">선택</option>{roles.map((role)=><option key={role}>{role}</option>)}</select></label></div><label><span>공식 출처</span><input name="sourceUrl" type="url" maxLength={500} placeholder="교회·교단·노회 공개 페이지"/></label><label><span>확인 내용</span><textarea name="note" minLength={5} maxLength={500} rows={3} placeholder="어느 페이지의 어떤 내용인지 적어 주세요." required/></label><input className="honeypot" name="company" tabIndex={-1} autoComplete="off" aria-hidden="true"/><button disabled={busy}>{busy?"접수 중…":"검토 요청 보내기"}</button>{message&&<p role="status">{message}</p>}</form></details>}
